export interface ISessionsRender {
    _id: any,
    code: string;
    mode: string;
    title: string;
    date: string;
}

export interface IConfig {
    themes: string[],
    language: string[],
}
